{
  "name": "visual-novel-dialogue-demo",
  "version": "1.0.0",
  "description": "Demo for Visual Novel Dialogue Plugin",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "clean": "rimraf dist",
    "build": "npm run clean && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@robingamedev/visual-novel-dialogue": "^0.5.1",
    "phaser": "^3.70.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^7.0.4",
    "rimraf": "^5.0.0"
  }
}
